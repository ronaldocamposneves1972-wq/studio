rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permite acesso total para qualquer usuário autenticado em todos os arquivos.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
