rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permite que qualquer usuário autenticado leia e escreva todos os arquivos.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
