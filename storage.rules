rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // ATENÇÃO: Temporariamente aberto para depuração.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
