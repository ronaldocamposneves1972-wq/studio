rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Permite leitura e escrita de todos os arquivos para qualquer usuário autenticado.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
