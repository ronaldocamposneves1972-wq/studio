rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Grant full read and write access to any authenticated user for all files.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
